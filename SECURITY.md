# 安全与隐私

不要提交 memory、backups、浏览器用户目录、简历、聊天日志、截图、HAR、Cookie、Codex 凭据或 Telegram token。日志和模型输入可能包含个人资料及 HR 消息，模型调用会把这些内容发给模型服务提供方。公开 issue 必须脱敏。

CDP 9222、VNC 5900、noVNC 6080 仅允许本地监听。noVNC 没有独立密码，只可通过 SSH 转发或已验证的身份认证网关访问；禁止直接开放到公网。脚本依赖本机用户隔离，不防御同机恶意用户。Chromium 应以非 root 用户运行，禁止为方便安装自动加 --no-sandbox。

模型输出并非事实保证。项目不做第二次 AI 审稿，也不以关键词拦截正文；真实履历、自然表达和未知事项交给本人确认等要求在 prompts 中。岗位 ID、动作结构、非空消息、运行租约、额度、防重和送达回执仍由代码核对。

发现漏洞请通过 GitHub 私密漏洞报告（如仓库已启用）联系维护者；否则先提交不含漏洞细节和凭据的 issue 请求安全联系方式。不要公开可利用细节或真实求职数据。

若令牌曾粘贴进公开日志、仓库或不可信渠道，应撤销并重新生成；删除文字不等于撤销凭据。
